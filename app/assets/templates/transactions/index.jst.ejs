<h2>All the transactions</h2>

<% transactions.each(function(trans) { %>

	<li> <%= trans.escape('payer_name') %> paid 
		<%= trans.escape('receiver_name') %> $
		<%= trans.escape('amount')        %> for
		<%= trans.escape('note')          %>
	</li>
<% }); %>

<!-- slider input type = range -->